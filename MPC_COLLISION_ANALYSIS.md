# MPC 碰撞问题分析

## 问题 1: 无人机闪现回原点

### CSV 数据分析
```
时间(s), uav_x, uav_y, uav_z, dist_min, path_length, infeasible_flag, collision_flag
63.74,  0.000,  0.000,  0.000,  5.11,    29.23,       0,                0
...
66.64,  0.000,  0.000,  0.000,  3.48,    29.23,       0,                0
```

**关键发现**：
- ✅ `dist_min` 正常工作（从 5.11 降到 3.48，障碍物在靠近）
- ❌ **无人机位置一直是 (0,0,0) - 没有移动！**
- ❌ `path_length` 固定在 29.23m（没有增加）
- ⚠️ `infeasible_flag` 一直是 0（但可能因为没真正飞行）

### 可能原因
1. **无人机还没起飞**
   - 等待目标点设置
   - 或者起飞失败

2. **MPC 没有真正执行轨迹**
   - 规划了但没有发送控制指令
   - 或者控制器没有响应

3. **里程计数据问题**
   - `data_recorder` 读不到正确的位置

---

## 问题 2: MPC 穿过椭球约束（硬约束失效）

### MPC 约束公式

从 `mpc_solver_setup.cpp` line 119:
```cpp
// 椭球避障约束（硬约束）
ocp.subjectTo(
    pow((x-obx[i])*cos(yaw[i])+(y-oby[i])*sin(yaw[i]), 2)/pow(obxsize[i],2) 
  + pow(-(x-obx[i])*sin(yaw[i])+(y-oby[i])*cos(yaw[i]), 2)/pow(obysize[i],2) 
  + pow((z-obz[i]), 2)/pow(obzsize[i],2) 
  - 1.0 + sk_d >= 0   // ← 关键：加了松弛变量 sk_d
);
```

**关键点**：
- 约束不是严格的椭球表面 `= 1.0`
- 而是 `≥ 1.0 - sk_d`
- **松弛变量 `sk_d` 允许违反约束！**

### 椭球大小计算

从 `mpcPlanner.cpp` line 1081-1083:
```cpp
osize[j](i,0) = dynamicObstaclesSize[i][j](0)/2 + this->dynamicSafetyDist_;
osize[j](i,1) = dynamicObstaclesSize[i][j](1)/2 + this->dynamicSafetyDist_;
osize[j](i,2) = dynamicObstaclesSize[i][j](2)/2 + this->dynamicSafetyDist_;
```

**椭球半径** = 障碍物半径 + 安全距离

### 默认参数

- `dynamicSafetyDist_` = **0.5m** (line 102)
- `staticSafetyDist_` = **0.3m** (默认值)
- `dynamicSlack_` = ? (松弛变量权重)

### 为什么会碰撞？

#### 原因 1: 松弛变量允许违反约束
```cpp
// 目标函数中惩罚松弛变量
cost += w_sk_d * sk_d²

// 但如果无路可走，MPC 会选择：
// "违反约束 + 付出松弛变量代价" > "完全无解"
```

**结果**：MPC 宁愿碰撞也要完成任务！

#### 原因 2: 安全距离太小
- 障碍物半径：`0.17m` (圆柱)
- 安全距离：`0.5m`
- **总椭球半径** = 0.17 + 0.5 = **0.67m**

如果无人机半径是 0.3m，实际安全余量只有：
```
0.67 - 0.3 = 0.37m
```

在高速相对运动中（1m/s），这个余量**非常小**！

#### 原因 3: 头对头场景无法避开
```
无人机:  →→→  (飞向 -16, 0)
障碍物:  ←←←  (从 -10 向 0 移动，速度 0.4m/s)
```

**在狭窄通道中正面相遇**：
- 左右躲避空间有限
- 上下躲避可能超出高度限制
- **MPC 发现无可行解，只能"硬闯"**

#### 原因 4: 优化时间窗口太短
- MPC horizon = 20 步（约 2 秒）
- 在高速接近时，2 秒内可能已经来不及规避
- 需要更早开始规避动作

### 解决方案

#### 方案 1: 增加安全距离（最直接）
```yaml
# trajectory_planner/cfg/mpc_param.yaml
dynamic_safety_dist: 1.0  # 从 0.5 改为 1.0
```

#### 方案 2: 增加松弛变量惩罚（更保守）
```cpp
// 在目标函数中增大松弛变量权重
// 让 MPC 更不愿意违反约束
w_sk_d = 1000.0  # 增大权重
```

#### 方案 3: 增加 horizon（提前规避）
```yaml
horizon: 30  # 从 20 增加到 30，提前 1 秒开始规避
```

#### 方案 4: 降低速度（给更多反应时间）
```yaml
max_vel: 0.8  # 从 1.0 降到 0.8
```

#### 方案 5: 改变场景（最根本）
- 不要设置正面对撞的场景
- 或者增加横向空间（障碍物不在正中间）
- 或者降低障碍物速度

### 为什么 infeasible_flag 没有显示？

**可能原因**：
1. MPC 虽然选择了碰撞路径，但**技术上仍然是"可行解"**
   - 因为松弛变量 `sk_d` 使约束放松了
   - OSQP 求解器认为找到了解（虽然违反了物理约束）

2. 只有当**完全无解**时才会 `infeasible`
   - 所有可能的轨迹都违反约束
   - OSQP 求解器返回失败

3. **碰撞≠无解**
   - 碰撞只是"代价高的可行解"
   - 不碰撞是"代价低的可行解"
   - MPC 会选择它认为最好的（即使会碰撞）

### 验证方法

#### 1. 检查松弛变量是否激活
查看 MPC 日志中 `sk_d` 的值：
- 如果 `sk_d > 0`，说明在违反约束
- 如果 `sk_d = 0`，说明没有违反

#### 2. 可视化 MPC 椭球
```bash
rostopic echo /mpc_planner/ellipsoid_obstacles
```

观察椭球是否真的包围了障碍物

#### 3. 降低障碍物速度测试
将 `velocity: 0.4` 改为 `velocity: 0.1`，看是否还会碰撞

---

## 总结

### 问题 1: 无人机闪现回原点
- **不是 MPC 崩溃**（没有 segfault 日志）
- **是无人机根本没移动**
- 需要检查为什么无人机位置一直是 (0,0,0)

### 问题 2: MPC 碰撞
- **硬约束不是真正"硬"的**（有松弛变量）
- **安全距离可能太小**（0.5m）
- **头对头场景本身就很难避开**
- **MPC 宁愿碰撞也要完成任务**

### 建议优先级
1. **先解决无人机不移动的问题**（否则无法测试）
2. **增加安全距离到 1.0m**
3. **如果还碰撞，修改场景（不要正面对撞）**
