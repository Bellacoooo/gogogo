# 数据分析报告

## 用户观察到的问题

1. ✅ **时间戳不对**：2026-05-15而不是2026-01-15
   - 这是系统时间设置问题，不影响算法逻辑

2. ⚠️ **部分概率为0**：很多行P_forward=0, P_left=0, P_right=0, P_stop=0
   - 这是严重的bug，说明意图概率没有正确计算或输出

3. ❌ **核心矛盾：ADE很小（1-2cm）但意图分布异常**
   - 如果预测准确（ADE小），意图应该集中
   - 但实际可能意图分散或不合理
   - **这说明ADE计算或意图计算有问题**

---

## 🔍 ADE计算问题分析

### 当前实现（第2113-2141行）

```cpp
for (int t = 0; t < validSteps; ++t) {
    // predTraj[t]是预测的第t步位置
    // refTraj[refIdx]是历史轨迹的第refIdx个点
    const int refIdx = refStartIdx + t;  // refStartIdx=0
    
    // 计算误差
    Eigen::Vector3d error = predTraj[t] - refTraj[refIdx];
    double dist = error.head<2>().norm(); // 2D距离
    ade += dist;
}
ade /= actualSteps;
```

### 关键问题：这是"回测"评估！

**正常的在线预测评估**：
- t0时刻：用history[0...t0]预测future[t1...tN]
- 等到tN时刻：比较predicted[t1...tN] vs actual[t1...tN]

**当前的"回测"评估**（怀疑）：
- 用当前历史轨迹history[0...N]作为"ground truth"
- 用当前状态+意图生成"预测"轨迹pred[0...N]
- 直接比较pred vs history

**为什么ADE会很小**：
1. 如果障碍物做匀速直线运动
2. 历史轨迹history = [p0, p1, p2, ...] 是一条直线
3. 用"forward"意图生成的预测：pred = [p0', p1', p2', ...]
4. 由于初始状态相同，预测方向也是直线
5. **pred ≈ history，所以ADE很小！**

这不是真正的预测能力评估，而是"拟合能力"评估！

---

## 🔍 意图分布问题分析

### 可能的原因

#### 原因A：s_adaptive还是太小（V3修复未生效）

**需要检查**：
- 数据中的s_adaptive值是多少？
- 是否接近4-5（期望值）？
- 还是仍然在1-3（失败）？

**如果s<3**：
- 意图惯性不足
- 容易受瞬时观测噪声影响
- 导致概率分布分散

#### 原因B：转移矩阵计算有问题

**可能的bug**：
- 我的V3修改引入了新问题？
- 或者原来就有bug？

#### 原因C：概率归一化问题

**从数据看**：很多行的4个概率都是0！
- 这不可能！概率和应该=1
- 说明概率计算或输出有严重bug

---

## 🔧 诊断步骤

### 步骤1：检查s_adaptive值

从您的数据中，找几行看s值：

```
# 需要从数据中提取第8列（s_adaptive）
# 如果s仍然<3，说明V3修复未生效或不够
# 如果s>4，但意图分布还是有问题，说明是其他原因
```

### 步骤2：检查概率和

```python
# 对每一行，计算：P_sum = P_forward + P_left + P_right + P_stop
# 正常应该 P_sum ≈ 1.0
# 如果P_sum = 0，说明概率计算失败
# 如果P_sum ≠ 1，说明归一化有问题
```

### 步骤3：检查日志输出

启用调试日志（第2174-2204行有详细输出）：

```bash
rostopic echo /rosout | grep "CSV写入"
```

应该能看到：
```
[INFO] CSV写入：Obs 0 位置=(x, y), P=[0.xx, 0.xx, 0.xx, 0.xx]
```

如果看到P=[0, 0, 0, 0]，说明intentProb_没有正确赋值。

---

## 💡 可能的修复方案

### 修复A：如果s还是太小

1. **增大a_change_max**（降低f_delta_a）：
   ```cpp
   double a_change_max = 5.0;  // 从2.0增大到5.0
   ```

2. **增强EMA平滑**：
   ```cpp
   double lambda = 0.9;  // 从0.8增大到0.9
   ```

3. **放宽归一化上限**：
   ```yaml
   # predictor_param.yaml
   alpha_max: 9.42  # 从π改为3π
   e_max: 1.0       # 从0.3改为1.0
   ```

### 修复B：如果概率计算失败

需要检查：
1. intentProb_是否正确赋值
2. 转移矩阵是否正确生成
3. 马尔可夫链迭代是否正常

### 修复C：如果ADE计算不合理

**建议**：改用真正的在线预测评估
- 缓存t0时刻的预测
- 在t0+N时刻，用实际轨迹评估
- 这才是真正的预测能力

---

## 📊 请提供以下信息以进一步诊断

### 1. 从数据中随机抽取几行，告诉我：

```
时间戳              ID  X      Y      Mt     s      intent  P_f    P_l    P_r    P_s
示例：2026-05-15 ... 0  -3.10  -1.07  0.15   4.5    0       0.89   0.05   0.04   0.02
```

**关键看**：
- s是否>4.0（V3是否生效）
- P值是否都是0（概率计算是否失败）
- Mt是否<0.3（指标是否正常）

### 2. 查看ROS日志

```bash
rostopic echo /rosout | grep -E "CSV写入|意图概率|Obs.*Intent"
```

告诉我看到了什么。

### 3. 障碍物的真实运动状态

- 障碍物是在做什么运动？（直线/转弯/静止）
- 速度大概多少？
- 运动是否规则？

---

## 🎯 总结

**核心问题**：
1. ✅ **ADE太小**可能是因为用"回测"而不是真正的预测评估
2. ❌ **意图分布异常**可能是因为：
   - s还是太小（V3修复不够或未生效）
   - 概率计算有bug（很多P都是0）
   - 转移矩阵生成有问题

**下一步**：
请提供数据中的s值和P值，我才能判断是哪个问题。

---

**分析日期**：2026-01-15  
**状态**：等待用户提供更多信息
