# test_stop.world 路径设计说明

## 设计目标

创建一个斜向移动的障碍物路径，用于验证意图预测和自适应s的准确性：
1. **斜着走10秒**（速度0.4 m/s，距离4米）
2. **缓慢停止**（减速段）
3. **停留3秒**（通过多个极短路径段模拟）
4. **继续斜着走**（恢复正常速度）

## 路径详细设计

### 第一段：斜着走10秒
- **起点**：(-10, -3, 0)
- **终点**：(-7.17, -0.17, 0)
- **方向**：斜向45度（东北方向）
- **距离**：4.0米（√(2.83² + 2.83²) = 4.0米）
- **速度**：0.4 m/s
- **耗时**：4.0 / 0.4 = **10秒** ✓
- **预期行为**：障碍物匀速斜向移动，意图预测应该识别为"前进"意图，自适应s应该保持较高值（接近s_max）

### 第二段：缓慢停止（减速段）
- **起点**：(-7.17, -0.17, 0)
- **终点**：(-7.099, -0.099, 0)
- **方向**：与第一段相同（斜向45度）
- **距离**：0.1米
- **速度**：0.4 m/s
- **耗时**：0.1 / 0.4 = **0.25秒**
- **预期行为**：障碍物开始减速，意图预测应该开始降低对"前进"意图的置信度，自适应s应该开始下降

### 第三段：停留3秒
- **起点**：(-7.099, -0.099, 0)
- **终点**：(-6.259, 0.741, 0)
- **方向**：与第一段相同（斜向45度）
- **实现方式**：24个极短路径段，每个0.05米
- **总距离**：24 × 0.05 = 1.2米
- **速度**：0.4 m/s
- **总耗时**：1.2 / 0.4 = **3秒** ✓
- **预期行为**：
  - 障碍物在很小区域内缓慢移动（模拟停留）
  - 意图预测应该识别为"停止"意图，概率应该显著增加
  - 自适应s应该下降到接近s_min（因为运动变化指标Mt应该增大）

### 第四段：继续斜着走
- **起点**：(-6.259, 0.741, 0)
- **终点**：(-3.429, 3.571, 0)
- **方向**：与第一段相同（斜向45度）
- **距离**：4.0米（√(2.83² + 2.83²) = 4.0米）
- **速度**：0.4 m/s
- **耗时**：4.0 / 0.4 = **10秒** ✓
- **预期行为**：
  - 障碍物从停止状态重新开始移动
  - 意图预测应该从"停止"切换回"前进"意图
  - 自适应s应该从s_min快速上升到s_max（因为运动变化指标Mt应该先增大后减小）

## 总时间线

| 时间段 | 行为 | 距离 | 耗时 | 预期意图 | 预期s值 |
|--------|------|------|------|----------|---------|
| 0-10s | 斜着走 | 4.0m | 10s | 前进（高概率） | s_max (≈5.0) |
| 10-10.25s | 缓慢停止 | 0.1m | 0.25s | 前进→停止（过渡） | s_max → s_min |
| 10.25-13.25s | 停留 | 1.2m | 3s | 停止（高概率） | s_min (≈1.2) |
| 13.25-23.25s | 继续走 | 4.0m | 10s | 停止→前进（过渡） | s_min → s_max |

**总耗时**：23.25秒

## 验证要点

### 1. 意图预测准确性
- **10秒时**：应该预测"前进"意图，概率应该最高（>0.5）
- **10.25-13.25秒**：应该预测"停止"意图，概率应该最高（>0.5）
- **13.25秒后**：应该从"停止"切换回"前进"意图

### 2. 自适应s值验证
- **0-10秒**（稳定运动）：
  - Mt应该接近0（f_jerk≈0, f_angular≈0, f_error≈0）
  - s_adaptive应该接近s_max（默认5.0）
  
- **10-10.25秒**（减速阶段）：
  - Mt应该开始增大（f_jerk增大，f_error增大）
  - s_adaptive应该开始下降
  
- **10.25-13.25秒**（停留阶段）：
  - Mt应该较大（f_jerk较大，f_error较大）
  - s_adaptive应该接近s_min（默认1.2）
  
- **13.25秒后**（重新开始移动）：
  - Mt应该先增大（从停止到移动的突变）
  - 然后逐渐减小（稳定移动后）
  - s_adaptive应该从s_min快速上升到s_max

### 3. 运动诊断指标验证
- **f_jerk（加加速度）**：
  - 稳定运动时：接近0
  - 减速/加速时：显著增大
  - 停留时：可能较大（速度变化）
  
- **f_angular（角加速度）**：
  - 斜向直线运动时：接近0（方向不变）
  
- **f_error（预测误差）**：
  - 稳定运动时：接近0（符合恒速模型）
  - 减速/停止时：显著增大（不符合恒速模型）
  - 重新启动时：显著增大（不符合恒速模型）

## 技术细节

### 坐标计算
所有路径段都沿斜向45度方向，计算公式：
- 斜向距离 = √(Δx² + Δy²)
- 对于斜向45度：Δx = Δy = 距离 / √2

### 插件限制
- `obstaclePathPlugin`不支持真正的"停留"（速度=0）
- 通过设置多个极短路径段（每个0.05米）模拟停留效果
- 障碍物会在1.2米的区域内缓慢移动，视觉上接近停留

### 优化建议
如果需要更精确的停留效果，可以考虑：
1. 使用更短的路径段（如0.01米），但需要更多waypoint
2. 修改插件代码，支持在waypoint停留指定时间
3. 使用多个重复waypoint（但orientation=false时不生效）

## 使用方法

1. 启动Gazebo仿真：
```bash
roslaunch uav_simulator start.launch world_name:=test_stop
```

2. 启动意图预测节点：
```bash
roslaunch dynamic_predictor dynamic_predictor.launch
```

3. 观察意图概率和自适应s值：
```bash
# 查看意图概率可视化
rostopic echo /dynamic_predictor/intent_probability

# 查看自适应s值
rostopic echo /dynamic_predictor/current_s_value

# 查看自适应指标
rostopic echo /dynamic_predictor/adaptive_metrics
```

4. 分析CSV日志（保存在桌面）：
- 查看ADE/FDE随时间的变化
- 查看s_adaptive与意图概率的关系
- 验证自适应机制是否按预期工作

---

**设计完成日期**: 2026-01-08  
**设计目的**: 验证自适应意图惯性机制的准确性  
**版本**: v1.0

