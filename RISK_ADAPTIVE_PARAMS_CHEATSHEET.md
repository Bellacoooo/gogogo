# 风险自适应椭球参数速查表

## 🎯 核心公式

```
s = clip(s0 + α·vc + β·exp(-ttc/τ), s_min, s_max)

其中：
  vc = max(0, -r̂·(v_r - v_i))  (closing speed)
  ttc = d / (vc + ε)            (time to collision)
  
椭球半轴：
  a = a0 + s(1 + κ)  (长轴，朝向障碍物运动方向)
  b = b0 + s(1 - κ)  (短轴，垂直方向)
  c = c0             (高度方向，不变)
```

---

## 📋 参数对照表

| 参数 | YAML 名称 | 默认值 | 范围 | 单位 | 含义 | 影响 |
|------|-----------|--------|------|------|------|------|
| **s0** | `risk_s0` | 0.15 | 0.1-0.3 | m | 基线膨胀量 | 所有情况下的最小风险裕量 |
| **α** | `risk_alpha` | 0.4 | 0.2-0.6 | - | closing speed 系数 | 逼近速度对膨胀的影响 |
| **β** | `risk_beta` | 0.6 | 0.4-0.8 | m | TTC 指数项系数 | 碰撞时间对膨胀的影响 |
| **τ** | `risk_tau` | 1.5 | 1.0-3.0 | s | TTC 衰减时间常数 | TTC 影响的持续距离 |
| **κ** | `risk_kappa` | 0.35 | 0.0-0.6 | - | 各向异性强度 | 椭球扁平程度（0=圆，1=线） |
| **s_min** | `risk_s_min` | 0.0 | 0.0-0.2 | m | 最小膨胀量 | 远距离/远离时的下限 |
| **s_max** | `risk_s_max` | 1.2 | 0.8-2.0 | m | 最大膨胀量 | 近距离/高速时的上限 |
| **v_th** | `risk_vel_threshold` | 0.15 | 0.1-0.3 | m/s | 低速阈值 | 低于此速度时朝向保持稳定 |

---

## 🎛️ 快速调参场景

### 场景 A：保守避障（拥挤环境、低速）

```yaml
mpc_planner/risk_s0: 0.25
mpc_planner/risk_alpha: 0.5
mpc_planner/risk_beta: 0.8
mpc_planner/risk_tau: 2.5
mpc_planner/risk_kappa: 0.25
mpc_planner/risk_s_max: 1.5
```

**特点**：大基线、强 TTC 反应、较弱各向异性  
**适用**：室内导航、人群穿行、狭窄通道

---

### 场景 B：激进穿行（开阔环境、高速）

```yaml
mpc_planner/risk_s0: 0.10
mpc_planner/risk_alpha: 0.3
mpc_planner/risk_beta: 0.5
mpc_planner/risk_tau: 1.2
mpc_planner/risk_kappa: 0.45
mpc_planner/risk_s_max: 1.0
```

**特点**：小基线、弱反应、强各向异性  
**适用**：户外飞行、稀疏障碍、快速机动

---

### 场景 C：均衡模式（推荐默认）

```yaml
mpc_planner/risk_s0: 0.15
mpc_planner/risk_alpha: 0.4
mpc_planner/risk_beta: 0.6
mpc_planner/risk_tau: 1.5
mpc_planner/risk_kappa: 0.35
mpc_planner/risk_s_max: 1.2
```

**特点**：平衡安全与效率  
**适用**：大多数场景

---

### 场景 D：对向交通（head-on 场景）

```yaml
mpc_planner/risk_s0: 0.15
mpc_planner/risk_alpha: 0.6    # 提高！
mpc_planner/risk_beta: 0.7     # 提高！
mpc_planner/risk_tau: 1.2      # 降低！
mpc_planner/risk_kappa: 0.50   # 提高！
mpc_planner/risk_s_max: 1.5
```

**特点**：对 closing speed 高度敏感、强各向异性  
**适用**：迎面冲突、对向运动

---

## 🔍 参数含义详解

### 1. s0（基线膨胀量）

- **物理意义**：静态风险，即使障碍物静止时也保持的安全裕量
- **调大**：整体更保守，所有情况下距离更远
- **调小**：更激进，允许更近距离通过
- **典型值**：0.1-0.25m

### 2. α（closing speed 系数）

- **物理意义**：每单位逼近速度带来的额外膨胀
- **调大**：对迎面冲突反应更强烈
- **调小**：允许更高速接近
- **典型值**：0.3-0.5

**示例**：
- `vc = 2 m/s, α = 0.4` → 额外膨胀 `0.4*2 = 0.8m`
- `vc = 2 m/s, α = 0.6` → 额外膨胀 `0.6*2 = 1.2m`

### 3. β（TTC 指数项系数）

- **物理意义**：时间紧迫性的权重
- **调大**：对即将碰撞的情况反应更强
- **调小**：减弱时间因素的影响
- **典型值**：0.5-0.8m

**示例**：
- `ttc = 1s, τ = 1.5, β = 0.6` → 额外膨胀 `0.6*exp(-1/1.5) ≈ 0.31m`
- `ttc = 3s, τ = 1.5, β = 0.6` → 额外膨胀 `0.6*exp(-3/1.5) ≈ 0.08m`

### 4. τ（TTC 衰减时间常数）

- **物理意义**：TTC 影响的持续距离/时间尺度
- **调大**：TTC 影响在更远距离仍然显著
- **调小**：只在非常近时才强烈反应
- **典型值**：1.5-2.5s

**示例**：
- `τ = 1.0s` → `ttc = 2s` 时衰减到 `exp(-2) ≈ 13.5%`
- `τ = 2.0s` → `ttc = 2s` 时衰减到 `exp(-1) ≈ 36.8%`

### 5. κ（各向异性强度）

- **物理意义**：椭球长轴与短轴的比例差异
- **调大**：椭球更扁平，方向性更强
- **调小**：椭球更圆，方向性更弱
- **典型值**：0.2-0.5
- **极限**：κ < 1（否则短轴会变负）

**示例**：
- `κ = 0, s = 0.6` → `Δ_∥ = 0.6, Δ_⊥ = 0.6` (圆形)
- `κ = 0.5, s = 0.6` → `Δ_∥ = 0.9, Δ_⊥ = 0.3` (扁椭圆，长短轴比 3:1)

### 6. s_max（最大膨胀量）

- **物理意义**：安全裕量的上限，防止过度膨胀
- **调大**：允许更大的安全裕量（更保守）
- **调小**：限制最大膨胀（更激进）
- **典型值**：1.0-1.5m

### 7. v_threshold（低速阈值）

- **物理意义**：低于此速度时，椭球朝向不变，避免抖动
- **调大**：更大范围内保持稳定（但响应可能延迟）
- **调小**：更快响应速度变化（但可能抖动）
- **典型值**：0.1-0.25 m/s

---

## 📊 参数影响矩阵

| 目标 | 增大参数 | 效果 |
|------|----------|------|
| **更保守（安全优先）** | s0, α, β, τ, s_max | 更大的安全距离，提前避让 |
| **更激进（效率优先）** | - | 减小上述参数 |
| **方向性更强** | κ | 椭球更扁，利用各向异性 |
| **更平滑稳定** | v_threshold, τ | 减少突变，更平缓过渡 |
| **快速反应** | α, β | 减小 τ | 对速度和时间变化敏感 |

---

## 🧪 验证方法

### 1. 检查 s 的范围

```python
# 最小值（远距离、vc=0）
s_min_expected = s0 ≈ 0.15m

# 最大值（近距离、高 vc）
# 例如：d=1m, vc=2m/s
ttc = 1/2 = 0.5s
s_raw = 0.15 + 0.4*2 + 0.6*exp(-0.5/1.5)
      = 0.15 + 0.8 + 0.6*0.717
      = 1.38m
s = min(1.38, 1.2) = 1.2m  # 被 s_max 限制
```

### 2. 检查各向异性比例

```python
# 例如：s=0.6m, κ=0.35
delta_parallel = 0.6 * (1 + 0.35) = 0.81m
delta_perp = 0.6 * (1 - 0.35) = 0.39m
ratio = delta_parallel / delta_perp = 2.08

# 长短轴比例：约 2:1
```

### 3. 运行分析脚本

```bash
python3 analyze_risk_adaptive.py
```

查看生成的图表，验证参数是否符合预期。

---

## ⚠️ 常见错误配置

### ❌ 错误 1：κ 过大

```yaml
mpc_planner/risk_kappa: 0.8  # 太大！
```

**问题**：短轴 `b = b0 + s(1-0.8) = b0 + 0.2s` 过小，椭球过扁，约束过紧  
**修复**：`κ ≤ 0.6`

### ❌ 错误 2：s_max 过小

```yaml
mpc_planner/risk_s_max: 0.5  # 太小！
```

**问题**：高速逼近时膨胀不足，可能碰撞  
**修复**：`s_max ≥ 1.0m`

### ❌ 错误 3：α 和 β 同时很大

```yaml
mpc_planner/risk_alpha: 0.8
mpc_planner/risk_beta: 1.0
```

**问题**：过度反应，任何接近都会导致巨大膨胀  
**修复**：平衡 α 和 β，通常 `α + β ≤ 1.2`

### ❌ 错误 4：τ 过小

```yaml
mpc_planner/risk_tau: 0.5  # 太小！
```

**问题**：TTC 影响衰减太快，只在极近距离才反应  
**修复**：`τ ≥ 1.0s`

---

## 📚 进一步阅读

- [完整实现文档](RISK_ADAPTIVE_ELLIPSOID_IMPLEMENTATION.md)
- [快速开始指南](QUICK_START_RISK_ADAPTIVE.md)

---

**版本**: v1.0  
**更新日期**: 2026-01-13

