# Intent-MPC 工程主要贡献概述

*基于代码库分析整理*

---

## 一、预测部分（Dynamic Predictor）

### 1. 基于MDP的意图识别

采用**马尔可夫决策过程（MDP）**框架进行意图推理，将动态障碍物的运动模式分为四类意图：前进（Forward）、左转（Left）、右转（Right）、停止（Stop）。通过观测历史运动轨迹（位置、速度、加速度），构建转移概率矩阵来更新意图概率分布。

**转移概率设计**：基于角度变化 $\theta$ 和速度模长 $r$ 设计转移向量，采用高斯分布建模前进概率，三角函数建模转向概率，双曲正切函数建模停止概率。

**核心创新**：提出**自适应意图惯性机制（Adaptive Intent Inertia）**。传统MDP使用固定的权重参数调节意图转移的惯性，本工作通过融合三个物理指标（加加速度、角加速度、预测误差）构建综合运动变化度量 $M_t$，并用Sigmoid函数动态映射到意图惯性权重 $s_{adaptive}$。该方法能在稳定运动时增强抗噪能力（高惯性，$s \to s_{max}$），在机动突变时快速响应意图切换（低惯性，$s \to s_{min}$）。

### 2. 基于意图的多模态轨迹采样

对每个识别出的意图，采用**采样-传播**方法生成多条候选轨迹（参考原论文Algorithm 1）：

- **Forward意图**：在线性加速度范围内采样多组控制输入，通过运动学模型前向传播生成轨迹
- **Left/Right意图**：同时采样线速度和角速度组合，通过非完整约束运动模型生成转向轨迹  
- **Stop意图**：将当前位置复制作为未来预测位置，同时膨胀风险尺寸

**轨迹融合**：通过`genTraj`函数计算所有采样轨迹的均值（mean）和方差（variance），形成高斯分布表示，输出为意图概率加权的多模态预测结果。

### 3. 静态障碍物碰撞检测与轨迹修正

**碰撞检测**：预测轨迹生成后，通过`positionCorrection`函数检查平均轨迹是否与静态地图碰撞（调用`map_->isInflatedOccupied()`）。

**轨迹修正**：若平均轨迹发生碰撞，从所有采样轨迹中选择距离平均轨迹最近的安全轨迹作为替代，确保预测结果的可行性。

---

## 二、规划部分（Trajectory Planner）

### 1. 概率风险地图（Probabilistic Risk Map）

**地图内容**：风险地图**仅包含动态障碍物**的预测轨迹风险，不包含静态障碍物。

**构建方法**：
- 遍历每个动态障碍物的所有意图模态
- 对每个预测时间步，按意图概率加权，施加时间衰减因子 $\gamma^t$
- 使用高斯核函数（马氏距离）将预测位置的协方差矩阵"盖章"到2D栅格上
- 累加所有障碍物、所有意图、所有时间步的风险值

**输出**：概率风险值归一化到 [0, 100] 的占据栅格地图，提供动态障碍物的空间风险分布。

### 2. 风险感知的全局路径规划（Risk-Aware A*）

**双地图输入**：
- **静态占据地图**：通过`map_->isInflatedOccupied()`查询，用于硬约束（不可通行区域）
- **动态风险地图**：通过`setRiskMap()`设置，用于软约束（高风险惩罚）

**A*改进**：
- 在传统A*代价函数中引入风险地图的概率值作为惩罚项
- 代价函数：$f = g + h + w_{risk} \cdot \text{risk}(pos)$
- 使全局路径能够在保证静态可行性的前提下主动规避动态障碍物的高风险区域

### 3. 模型预测控制（MPC with OSQP Solver）

**动力学模型**：采用**双积分器模型**（Double Integrator），状态为位置+速度，控制输入为加速度。

**离散化**：
```
x_{k+1} = A x_k + B u_k
A = [I, I*Δt; 0, I],  B = [0.5*Δt²*I; Δt*I]
```

**求解器**：使用**OSQP**（Operator Splitting Quadratic Program）求解二次规划问题。

**约束**：
- 速度/加速度限制
- 地面/天花板高度限制  
- 静态障碍物避障约束（固定安全距离）
- 动态障碍物避障约束（风险自适应椭球）

**核心创新**：提出**风险自适应椭球约束（Risk-Adaptive Ellipsoidal Constraints）**。

传统MPC使用固定的圆形或椭球安全距离，本工作根据机器人与障碍物的相对运动状态动态调整椭球参数：

- **危险程度量化**：
  - Closing speed（逼近速度）：$v_c = \max(0, -\hat{r} \cdot (v_r - v_i))$
  - Time-to-Collision（碰撞时间）：$\text{TTC} = d / (v_c + \epsilon)$

- **自适应膨胀量**：
  $$s = \text{clip}(s_0 + \alpha \cdot v_c + \beta \cdot e^{-\text{TTC}/\tau}, s_{min}, s_{max})$$
  高风险时自动增大安全裕量

- **朝向与各向异性**：
  - 椭球长轴朝向障碍物运动方向：$\phi = \arctan2(v_{i,y}, v_{i,x})$
  - 各向异性分配：$\Delta_\parallel = s(1+\kappa)$，$\Delta_\perp = s(1-\kappa)$
  - 危险方向（长轴）保持更大余量，垂直方向（短轴）留出机动空间

- **稳定性机制**：
  - 低通滤波平滑参数变化
  - 变化率限制防止突变
  - 低速时退化为圆形避免抖动

### 4. Top-K轨迹筛选与评分

对MPC生成的候选轨迹进行**多目标评分**：

- **参考路径跟踪误差**：与A*全局路径的偏离程度
- **轨迹平滑度**：加速度/加加速度惩罚
- **安全性**：与障碍物最小距离和风险地图累积概率

通过加权评分选择最优轨迹，在跟踪全局路径、保证平滑性和确保安全性之间取得平衡。

---

## 三、系统整体贡献

### 预测侧创新

在MDP意图识别框架基础上，创新性地引入基于多物理量融合的自适应惯性权重机制，并通过采样-传播方法生成多模态概率化的预测轨迹分布，同时考虑与静态环境的交互约束。

### 规划侧创新

采用双积分器模型和OSQP求解器实现MPC，突破传统固定安全距离的局限，提出风险感知的动态椭球约束方法，使规划器能够根据交互场景的危险程度（相对速度、碰撞时间）自适应调整避障策略。

### 系统协同

实现了"预测-规划-控制"的闭环协同：
1. 预测模块输出多模态轨迹→生成概率风险地图
2. A*全局规划同时考虑静态地图+动态风险地图
3. MPC局部规划根据实时相对运动状态自适应调整约束
4. 将动态障碍物的意图信息显式整合到轨迹优化约束中

通过这一完整框架，显著提升了无人机在密集动态环境中的导航安全性和通行效率。

---

## 技术栈总结

| 模块 | 方法 | 关键技术 |
|------|------|---------|
| 意图识别 | MDP | 自适应惯性权重（Sigmoid映射） |
| 轨迹预测 | 采样-传播 | 多模态高斯分布、碰撞检测 |
| 风险地图 | 概率栅格 | 高斯核盖章、时间衰减 |
| 全局规划 | A* | 双地图（静态硬约束+动态软约束） |
| 局部规划 | MPC | 双积分器、OSQP、风险自适应椭球 |
| 轨迹评分 | 多目标优化 | 路径跟踪、平滑度、安全性 |

---

*本文档基于代码库 `/home/ff/intent-mpc/src/Intent-MPC/` 分析整理，与IEEE RA-L 2025论文对应*

