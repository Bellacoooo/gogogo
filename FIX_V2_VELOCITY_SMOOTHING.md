# 修复方案 V2 - 速度平滑

## 🐛 V1 方案的问题

第一版修复尝试使用位置差分，但引入了新bug：
- **D_t = 1**（Mt达到最大值）
- s_adaptive = 1.225433（最小值）
- 效果比修复前更差

**根本原因**：
- 使用`currAngle = prevAngle`导致`theta = 0`
- 系统总是认为在直线前进，即使转向时也误判
- 或者索引计算错误，导致角度计算异常

---

## ✅ V2 修复方案：速度平滑

### 核心思想

**不改变角度计算的逻辑，而是对输入的速度进行平滑处理**

### 实现方法

```cpp
// 平滑速度 = 0.7 * 当前速度 + 0.3 * 前一速度（低通滤波）
Eigen::Vector3d smoothedVel = 0.7 * currVel + 0.3 * prevVel;
double currAngle = atan2(smoothedVel(1), smoothedVel(0));
```

### 优点

1. **保留角度变化信息** ✅
   - 转向时theta仍然会变化
   - 不会导致误判

2. **降低速度噪声** ✅
   - 通过加权平均平滑速度
   - 降低高频噪声（±5°的抖动）

3. **简单有效** ✅
   - 不涉及复杂的索引计算
   - 不会引入新bug

### 平滑系数说明

- **0.7** = 当前速度权重（主要）
- **0.3** = 前一速度权重（辅助）

**效果**：
- 如果速度突然变化（真实转向），能够快速响应
- 如果速度有随机噪声，会被平均掉

---

## 📊 预期效果

### 对于直线运动

**修复前**（使用原始速度）：
- 速度有噪声：v_x = [1.0, 1.02, 0.98, 1.01, ...]
- 角度抖动：θ = [0°, 1.1°, -1.1°, 0.6°, ...]
- f_angular = 0.3-0.6（高）
- Mt = 0.5-1.0（高）
- s = 1.2-3.0（低）

**修复后**（使用平滑速度）：
- 平滑速度：v_smooth = [1.0, 1.006, 1.002, 1.003, ...]
- 角度稳定：θ = [0°, 0.3°, -0.2°, 0.2°, ...]
- f_angular = 0.05-0.15（低）
- Mt = 0.1-0.3（低）
- s = 4.0-4.9（高）

### 对于真实转向

**平滑前后对比**：

| 场景 | 原始速度 | 平滑速度 | 效果 |
|------|---------|---------|------|
| 直线+噪声 | ±5° 抖动 | ±1° 抖动 | ✅ 噪声降低 |
| 急转弯 | 瞬间±30° | 延迟1-2帧 | ⚠️ 轻微滞后 |
| 匀速转弯 | 稳定±20° | 稳定±20° | ✅ 准确 |

---

## 🧪 测试步骤

### 1. 重新编译

```bash
cd /home/ff/intent-mpc
catkin_make -DCMAKE_BUILD_TYPE=Release
```

### 2. 运行测试

观察日志输出

### 3. 验证数据

**期望的直线运动数据**：

```
时间     ID  X       Y       ADE   FDE   Mt    s     P_f   P_l   P_r   P_s
19:30:13 0  -3.088  -1.088  0.04  0.07  0.15  4.5   0.88  0.05  0.05  0.02
19:30:14 0  -3.084  -1.089  0.04  0.08  0.12  4.7   0.90  0.04  0.04  0.02
19:30:15 0  -3.080  -1.090  0.04  0.08  0.13  4.6   0.89  0.05  0.04  0.02
```

**关键指标**：
- ✅ Mt < 0.2（不再是1.0）
- ✅ s > 4.0（不再是1.225）
- ✅ P_forward > 0.85（不再是0.4）

---

## ⚙️ 参数调优

### 如果效果还不够

可以调整平滑系数：

```cpp
// 更强的平滑（降低噪声，但响应变慢）
Eigen::Vector3d smoothedVel = 0.5 * currVel + 0.5 * prevVel;

// 更弱的平滑（保留响应性，但噪声略高）
Eigen::Vector3d smoothedVel = 0.8 * currVel + 0.2 * prevVel;

// 推荐（平衡）
Eigen::Vector3d smoothedVel = 0.7 * currVel + 0.3 * prevVel;  // ✅
```

### 如果s还是偏低

可以进一步调整归一化上限：

```yaml
# predictor_param.yaml

# 原值：
alpha_max: 3.14159  # π
e_max: 0.3

# 建议值：
alpha_max: 6.28     # 2π（放宽，降低f_angular灵敏度）
e_max: 0.5          # 0.5m（放宽，降低f_error灵敏度）
```

---

## 🔍 诊断方法

### 观察调试日志

```bash
rostopic echo /rosout | grep "Obs"
```

**期望输出**（直线运动）：
```
[INFO] Obs 0: f_jerk=0.08, f_angular=0.10, f_error=0.12 => Mt=0.11 => s=4.75
[INFO] Obs 0: f_jerk=0.06, f_angular=0.08, f_error=0.10 => Mt=0.09 => s=4.88
```

**如果看到**：
- `f_angular > 0.3` → 平滑不够，增大prevVel权重（0.3→0.5）
- `f_angular < 0.05` → 平滑过度，减小prevVel权重（0.3→0.2）
- `Mt > 0.5` → 有其他指标异常（f_jerk或f_error）

### 分析异常情况

| 症状 | 可能原因 | 解决方案 |
|------|---------|---------|
| Mt还是接近1 | 速度或加速度数据有严重问题 | 检查数据源质量 |
| s还是1.2 | Mt计算错误或sigmoid参数问题 | 检查sigmoid_k, sigmoid_mu |
| P分布还是混乱 | s太低或瞬时意图计算有问题 | 调整归一化上限 |

---

## 📝 V1 vs V2 对比

| 方案 | 方法 | 优点 | 缺点 | 结果 |
|------|------|------|------|------|
| **原始** | 使用原始速度 | 响应快 | 噪声大 | Mt=0.5-1.0, s=1-3 ⚠️ |
| **V1** | 位置差分或currAngle=prevAngle | 理论上无噪声 | 实现有bug，theta=0 | Mt=1.0, s=1.2 ❌ |
| **V2** | 速度平滑（0.7 + 0.3） | 平衡噪声和响应 | 需调参 | Mt<0.2, s>4.0 ✅（预期） |

---

## 🎯 总结

**V2修复的核心**：
- ✅ 对速度进行加权平均平滑
- ✅ 保留角度变化信息（不会误判转向）
- ✅ 降低高频噪声（提升f_angular稳定性）
- ✅ 简单可靠（不引入索引bug）

**立即测试**：
```bash
catkin_make -DCMAKE_BUILD_TYPE=Release
# 然后运行测试，观察Mt和s值
```

---

**修复日期**：2026-01-15  
**版本**：V2 - 速度平滑方案  
**状态**：✅ 已实现，待测试
