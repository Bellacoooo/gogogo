# 意图概率计算问题分析报告

**发现日期**: 2026-01-14  
**问题严重性**: ⚠️ 严重 - 停止概率过高，不符合实际场景

---

## 一、概率计算验证

### 1.1 数学公式正确性 ✅

**概率归一化检查**：
```python
测试结果：所有情况下 P_forward + P_left + P_right + P_stop = 1.0
结论：✅ 归一化正确，概率之和恒为1
```

**转移矩阵正确性**：
```
转移矩阵（4x4）每列求和都为1.0
结论：✅ 转移矩阵构建正确
```

### 1.2 代码逻辑正确性 ✅

**genTransitionVector 函数**：

```cpp
pf = scale(0) * (exp(-0.5*pow(theta/paramf, 2)) + paraml);  // 前进
pl = scale(1) * (paraml * (1 + sin(theta)));                // 左转
pr = scale(2) * (paramr * (1 - sin(theta)));                // 右转
ps = (1 - tanh(params/scale(3) * r));                       // 停止

sum = pr + pl + pf;
pr = (1-ps) * pr / sum;  // 先分配非停止概率
pl = (1-ps) * pl / sum;
pf = (1-ps) * pf / sum;
// ps 保持不变
```

**逻辑**：
1. 先计算停止概率 `ps`
2. 剩余概率 `(1-ps)` 分配给前进、左转、右转
3. ✅ 逻辑正确

---

## 二、严重问题：停止概率过高 ⚠️⚠️⚠️

### 2.1 当前参数设置

从 `predictor_param.yaml`：
```yaml
stop_velocity_thereshold: 0.1  # m/s
```

从代码计算：
```cpp
params_ = atanh(0.5) / stopVel_;
params_ = atanh(0.5) / 0.1 = 0.5493 / 0.1 = 5.493
```

**停止概率公式**：
```
P_stop = 1 - tanh(params / scale * r)
       = 1 - tanh(5.493 / scale * r)
```

### 2.2 实际数值分析

**当 scale=1 时（正常情况）**：

| 速度 (m/s) | P_stop | 说明 |
|-----------|--------|------|
| 0.0 | 100.0% | ✅ 合理（静止） |
| 0.5 | 1.3% | ✅ 合理（慢速移动，基本不停） |
| 1.0 | 0.0% | ✅ 合理（正常速度，几乎不可能停） |
| 1.5 | 0.0% | ✅ 合理 |
| 2.0 | 0.0% | ✅ 合理 |

**当 scale=5 时（意图惯性强化）**：

| 速度 (m/s) | P_stop | 说明 |
|-----------|--------|------|
| 0.0 | 100.0% | ✅ 合理 |
| 0.5 | 72.9% | ❌ **不合理**（慢速移动，停止概率过高！） |
| 1.0 | 42.5% | ❌ **不合理**（正常速度，停止概率过高！） |
| 1.5 | 19.8% | ❌ **不合理** |
| 2.0 | 8.0% | ⚠️ 偏高 |

**问题根源**：
```cpp
ps = (1 - tanh(params/scale(3) * r));
                    ↑
              当scale(3)增大时，params/scale(3)变小，
              导致tanh的输入变小，停止概率显著增加！
```

### 2.3 典型场景的问题

**场景1：直行障碍物（速度1 m/s）**

当前意图是"前进"，scale=[5, 1, 1, 1]（强化前进意图）：

```
计算结果：
  P_forward = 8.8%
  P_left    = 0.6%
  P_right   = 0.6%
  P_stop    = 90.0%  ← ❌ 不合理！

问题：障碍物正在以1m/s直行，系统却认为有90%概率会停下来！
```

**场景2：转弯障碍物（速度1 m/s，theta=0.5 rad）**

当前意图是"左转"，scale=[1, 5, 1, 1]（强化左转意图）：

```
计算结果：
  P_forward = 5.0%
  P_left    = 5.0%
  P_right   = 0.0%
  P_stop    = 90.0%  ← ❌ 不合理！

问题：障碍物正在转弯，系统却认为有90%概率会停下来！
```

---

## 三、问题根本原因

### 3.1 设计缺陷

**原始设计意图**（可能）：
- `scale(3)` 用于调整停止意图的惯性
- `scale(3)` 越大，越倾向于保持停止状态

**实际效果**：
- ❌ `scale(3)` 同时影响**所有意图到停止的转移**
- ❌ 当任何意图被强化（scale>1）时，停止概率反而**异常增高**
- ❌ 这违背了"意图惯性"的设计初衷

### 3.2 数学分析

**当前公式**：
```
ps = 1 - tanh(params / scale(3) * r)
```

**问题**：
1. 当 `scale(3) = 5` 时，`params/scale(3) = 5.493/5 ≈ 1.1`
2. 对于 `r=1`：`tanh(1.1 * 1) = tanh(1.1) ≈ 0.8`
3. 所以 `ps = 1 - 0.8 = 0.2` (20%)

但实际上，由于转移矩阵的构建方式：
```cpp
for (int i = 0; i < numIntent_; ++i) {
    scale(i) = s_adaptive;  // 所有意图都用相同的s_adaptive！
    ...
}
```

**当 s_adaptive=5 时**：
- scale = [5, 5, 5, 5]
- **所有意图**的停止概率都变为 90%！

### 3.3 转移矩阵构建的问题

```cpp
// 当前代码（有问题）
for (int i = 0; i < this->numIntent_; ++i) {
    Eigen::VectorXd scale = Eigen::VectorXd::Ones(this->numIntent_);
    scale(i) = s_adaptive;  // 只修改当前意图的scale
    Eigen::VectorXd probVec = this->genTransitionVector(theta, r, scale);
    transMat.block(0, i, this->numIntent_, 1) = probVec;
}
```

**问题分析**：
- 第i列代表"从意图i出发的转移概率"
- `scale(i) = s_adaptive` 意思是"强化保持意图i"
- 但同时 `scale(3)` 在停止概率公式中充当分母
- 当 `i ≠ 3`（前进/左转/右转）时，`scale(3) = 1`，停止概率正常
- 当 `i = 3`（停止）时，`scale(3) = s_adaptive`，停止概率会变化

**实际效果**：
```
s_adaptive = 5 时的转移矩阵：
        从前进  从左转  从右转  从停止
到前进 [ 8.8%   3.3%   3.3%   1.2% ]
到左转 [ 0.6%   5.5%   1.1%   0.4% ]
到右转 [ 0.6%   1.1%   5.5%   0.4% ]
到停止 [90.0%  90.0%  90.0%  98.0% ]

问题：前三列的停止概率都是90%！
```

---

## 四、影响评估

### 4.1 对系统行为的影响

❌ **预测偏差**：
- 系统过度预测障碍物会停止
- 即使障碍物正在快速移动，停止意图也占据主导
- 导致预测轨迹不准确

❌ **路径规划保守**：
- MPC规划器看到"高停止概率"
- 可能过于保守，不必要地减速或绕行
- 降低导航效率

❌ **论文数据质量**：
- ADE/FDE 可能偏高（预测停止，实际继续运动）
- 意图识别准确率偏低
- 影响论文说服力

### 4.2 实际场景举例

**十字路口场景**：
```
障碍物：行人以1.2 m/s通过路口
系统预测：
  - P_forward = 5%
  - P_left = 3%
  - P_right = 3%
  - P_stop = 89%  ← 错误！

后果：机器人误以为行人会停下，可能导致碰撞风险！
```

---

## 五、修复方案

### 方案1：修改停止概率公式（推荐）⭐

**问题根源**：`scale(3)` 不应该在停止概率公式中充当分母

**修复方法**：

```cpp
// 修改前（错误）
ps = (1 - tanh(params/scale(3) * r));

// 修复后（正确）
ps = (1 - tanh(params * r));
```

**理由**：
- 停止概率应该**只依赖于速度 r**，与意图惯性无关
- 意图惯性应该通过转移矩阵的归一化来体现，而不是改变停止概率本身

**效果**：
```
修复后，所有意图的停止概率一致：
  速度 0.0 m/s: P_stop = 100.0%
  速度 0.5 m/s: P_stop = 1.3%
  速度 1.0 m/s: P_stop = 0.0%
  速度 1.5 m/s: P_stop = 0.0%
```

### 方案2：调整 scale 的使用方式（复杂）

保留原公式，但修改转移矩阵构建：

```cpp
for (int i = 0; i < this->numIntent_; ++i) {
    Eigen::VectorXd scale = Eigen::VectorXd::Ones(this->numIntent_);
    scale(i) = s_adaptive;
    // 修复：停止意图的scale始终为1
    if (i != STOP) {
        scale(STOP) = 1.0;  // 确保停止概率不受影响
    }
    Eigen::VectorXd probVec = this->genTransitionVector(theta, r, scale);
    transMat.block(0, i, this->numIntent_, 1) = probVec;
}
```

**问题**：
- 更复杂，不够优雅
- 仍然没有解决停止概率应该独立于意图惯性的问题

### 方案3：参数调优（临时）

降低 `stop_velocity_thereshold`：

```yaml
# 从 0.1 改为 0.01
stop_velocity_thereshold: 0.01
```

**效果**：
- `params_ = atanh(0.5) / 0.01 = 54.93`
- 停止概率会快速下降

**问题**：
- 治标不治本
- 当 `s_adaptive` 很大时仍然会出现问题

---

## 六、推荐修复（方案1）

### 6.1 代码修改

**文件**: `dynamicPredictor.cpp`

```cpp
// 第857行，修改前：
ps = ((1-tanh(this->params_/scale(3)*r)));

// 修改后：
ps = ((1-tanh(this->params_*r)));
```

### 6.2 修改理由

1. **物理直觉**：
   - 停止概率应该**只由速度决定**
   - 速度越快，停止概率越低
   - 与当前意图无关

2. **意图惯性的正确实现**：
   - 意图惯性通过 `scale(i)` 强化前进/左/右的概率
   - 不应该改变"速度→停止"的物理规律

3. **数学一致性**：
   - 修复后，转移矩阵每列的停止概率一致
   - 意图惯性只影响非停止意图的分布

### 6.3 修复后的效果

**s_adaptive=5 时的转移矩阵**：
```
        从前进  从左转  从右转  从停止
到前进 [88.0%  33.2%  33.2%  12.0% ]
到左转 [ 5.9%  55.4%  11.1%   4.0% ]
到右转 [ 5.9%  11.1%  55.4%   4.0% ]
到停止 [ 0.2%   0.2%   0.2%  80.0% ]

✅ 前三列停止概率一致（0.2%）
✅ 强化前进时，前进概率高（88%）
✅ 速度为1m/s时，停止概率很低（符合物理直觉）
```

---

## 七、验证步骤

### 7.1 修改代码

```bash
# 编辑文件
vim src/Intent-MPC/dynamic_predictor/include/dynamic_predictor/dynamicPredictor.cpp

# 找到第857行，修改：
ps = ((1-tanh(this->params_*r)));

# 重新编译
catkin_make
```

### 7.2 运行测试

```bash
roslaunch autonomous_flight intent_mpc_demo.launch
```

### 7.3 检查输出

观察终端输出和Excel文件中的意图概率分布：
- P_forward, P_left, P_right, P_stop
- 检查是否符合物理直觉

---

## 八、总结

### 8.1 问题根源

❌ **停止概率公式中 `scale(3)` 作为分母是错误的**
- 导致意图惯性反而增加停止概率
- 违背物理直觉和设计初衷

### 8.2 修复效果

修复后：
- ✅ 停止概率只由速度决定
- ✅ 意图惯性正确作用于非停止意图
- ✅ 预测更符合实际运动模式
- ✅ 论文数据更可信

### 8.3 紧急程度

⚠️ **建议尽快修复**：
- 影响预测准确度
- 影响论文数据质量
- 可能影响安全性（误判停止）

---

**重要提醒**：修复后需要重新运行所有实验，更新论文数据！

