# A* 对角线路径修复报告

## 🐛 问题描述

用户反馈：**A* 可以走斜线，但常常还是走横线竖线，偶尔才走斜线，明明斜线更近**

## 🔍 根本原因分析

### 原因 1：使用了 6 邻域 ❌

我在之前的性能优化中将配置改为：
```yaml
astar/use_26_dir: false  # 6邻域
```

**6 邻域的限制**：
- 只有 6 个方向：±x, ±y, ±z（上下左右前后）
- **完全不能走对角线！**
- 所有路径都是横竖折线（Manhattan距离）

**26 邻域的优势**：
- 有 26 个方向：所有坐标轴方向 + 面对角线 + 体对角线
- **可以走斜线，路径更短**
- 欧几里得距离接近最优路径

### 原因 2：tie-breaking 的坐标字典序比较 ⚠️

原代码在 tie-breaking 时使用了坐标字典序比较：
```cpp
// 按字典序比较坐标
if (na.x != nb.x) return na.x > nb.x;  // 偏好某个x方向！
if (na.y != nb.y) return na.y > nb.y;  // 偏好某个y方向！
return na.z > nb.z;
```

**问题**：
- 这会导致 A* **系统性地偏好某些方向**（沿坐标轴）
- 即使使用 26 邻域，也会倾向于选择横竖路径而不是对角线
- 导致"偶尔才走斜线"的现象

### 原因 3：tie-breaker 因子太小 📏

原始的 tie-breaker：
```cpp
const double tie_breaker = 1.0 + 1.0 / 10000.0;  // 1.0001
```

**问题**：
- 提升太小（只有 0.01%），在浮点数精度范围内几乎没有效果
- 不足以打破横竖路径和对角线路径的平衡

## ✅ 修复方案

### 修复 1：启用 26 邻域

**文件**：`planner_param.yaml`

```yaml
# 修改前
astar/use_26_dir: false

# 修改后
astar/use_26_dir: true  # ✅ 启用对角线移动
```

### 修复 2：增大 tie-breaker 因子

**文件**：`a_star_occ.cpp`

```cpp
// 修改前
const double tie_breaker = 1.0 + 1.0 / 10000.0;  // 1.0001 (太小)

// 修改后
const double tie_breaker = 1.001;  // 1.001 (0.1% 提升，更有效)
```

**效果**：
- 0.1% 的提升在大多数情况下足以打破平衡
- 不会破坏启发式的可容许性（admissible）
- 让 A* 更倾向于直线路径

### 修复 3：移除坐标字典序 tie-breaking

**文件**：`a_star_occ.cpp`

```cpp
// 修改前（有方向偏好）
if (na.x != nb.x) return na.x > nb.x;
if (na.y != nb.y) return na.y > nb.y;
return na.z > nb.z;

// 修改后（使用key，无方向偏好）
return a.second > b.second;
```

**优点**：
- 不再系统性偏好某个方向
- 对角线和横竖线在相同代价下有公平的机会
- 路径选择更符合几何最优

### 修复 4：简化 tie-breaking 逻辑

完整的新 tie-breaking 逻辑：

```cpp
auto cmp = [&](const std::pair<double, long long> &a, const std::pair<double, long long> &b) {
    // 1. 比较 f 值（主要标准）
    if (std::abs(a.first - b.first) > 1e-12) {
        return a.first > b.first;
    }
    
    // 2. f 相同时，偏好 g 大的（h 小的，更接近目标）
    double g_a = gscore.count(a.second) ? gscore[a.second] : 0.0;
    double g_b = gscore.count(b.second) ? gscore[b.second] : 0.0;
    if (std::abs(g_a - g_b) > 1e-12) {
        return g_a < g_b;  // g 大的优先
    }
    
    // 3. 最后使用 key（避免方向偏好）
    return a.second > b.second;
};
```

## 📊 邻域类型对比

| 特性 | 6 邻域 | 26 邻域 |
|------|--------|---------|
| 方向数 | 6 | 26 |
| 可走对角线 | ❌ 不能 | ✅ 可以 |
| 路径长度 | 较长（Manhattan） | 较短（接近欧几里得） |
| 扩展节点数 | 少 | 多（~4.3x） |
| 规划速度 | 快 | 较慢 |
| 路径质量 | 差（折线） | 好（平滑） |
| **推荐用途** | 简单场景、速度优先 | **复杂场景、质量优先** |

## 🎯 预期效果

### 修复前 ❌
- 路径多为横竖折线
- 偶尔才走对角线
- 路径长度不是最优
- 明显不符合几何直觉

### 修复后 ✅
- **路径会优先选择对角线**（当它更短时）
- 路径长度接近欧几里得距离
- 路径更平滑、更符合直觉
- 在开阔空间中接近直线

### 示例对比

假设从 (0, 0, 0) 到 (10, 10, 0)：

**6 邻域路径**：
```
(0,0) → (1,0) → (2,0) → ... → (10,0) → (10,1) → ... → (10,10)
总长度：20 步
```

**26 邻域路径**：
```
(0,0) → (1,1) → (2,2) → ... → (10,10)
总长度：10√2 ≈ 14.14 步（短 29%！）
```

## 🧪 测试建议

### 简单测试场景

在一个开阔空间中，设置对角线上的起点和终点：
- 起点：(0, 0, 1.5)
- 终点：(10, 10, 1.5)

**预期路径**：
- ✅ 应该是对角线（或接近对角线）
- ✅ 不应该是横竖折线

### 查看路径

```bash
# 查看 A* 原始路径
rostopic echo /astar/planned_path

# 或在 RViz 中可视化
# Path topic: /astar/planned_path
```

### 验证对角线移动

检查路径中相邻点的坐标差：
- **横竖移动**：dx=0 或 dy=0 或 dz=0
- **对角线移动**：dx≠0 且 dy≠0（2D对角线）或 dx≠0 且 dy≠0 且 dz≠0（3D对角线）

## ⚖️ 性能权衡

### 使用 26 邻域的代价

- **规划时间增加**：约 2-4 倍（取决于场景复杂度）
- **内存使用增加**：展开更多节点

### 何时使用 6 邻域

如果满足以下条件，可以考虑改回 6 邻域：
1. 规划速度是**关键瓶颈**
2. 路径质量要求**不高**
3. 后续有**路径平滑**步骤
4. 场景**非常复杂**（障碍物密集）

### 何时使用 26 邻域（推荐）

如果满足以下条件，应该使用 26 邻域：
1. 需要**最短路径**
2. 路径质量**很重要**
3. 规划速度**可以接受**
4. 开阔空间**较多**

## 📝 配置调优

### 如果对角线移动还是不够多

可以进一步增大 tie-breaker：

```cpp
const double tie_breaker = 1.01;  // 1% 提升（更强）
```

### 如果路径太激进（过度偏好对角线）

可以减小 tie-breaker：

```cpp
const double tie_breaker = 1.0001;  // 0.01% 提升（更温和）
```

### 如果性能还是问题

可以调整最大展开节点数：

```yaml
astar/max_expanded_nodes: 100000  # 从 300000 降低
```

## 🎯 总结

| 问题 | 根本原因 | 修复方案 | 预期改善 |
|------|---------|---------|---------|
| 常走横竖，少走斜线 | 使用 6 邻域（不能走对角线） | 启用 26 邻域 | 可以走对角线 |
| 偶尔才走斜线 | tie-breaking 坐标字典序偏好 | 移除坐标比较 | 无方向偏好 |
| 对角线选择不够积极 | tie-breaker 因子太小 | 增大到 0.1% | 更倾向直线 |

---

**修复日期**：2026-01-15  
**问题发现者**：用户反馈  
**状态**：✅ 已完成，待测试验证

**关键点**：26 邻域是能够走对角线的前提！
